import { useRouter } from "next/router";

const Content = ({ content }) => {
  <div>
    <div>{youtubeName}</div>
    <iframe src={youtubeLink} frameBorder="0" width="300" height="300"></iframe>
  </div>;
};
export default Content;
